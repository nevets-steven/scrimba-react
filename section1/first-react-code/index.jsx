// 1. create a root

// 2. render some markup to the root

// script in html are importing packagess

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<h1>Hello from React!</h1>);